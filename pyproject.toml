[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "demo-actions"
version = "0.1.0"
description = "PatchOps live demo: a deliberately-failing, fixable CI project."
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
packages = ["demo_pkg"]
